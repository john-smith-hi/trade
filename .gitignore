__pycache__/
*.pyc
*.pyo
*.pyd
.Python
build/
dist/
history_mt5.txt

# Chứa login/password MT5 thật, không commit. Xem xml/accounts.example.xml để lấy mẫu.
xml/accounts.xml
xml/paths.xml

# Đường dẫn WAMP www trên máy local, không commit. Xem xml/www.example.xml.
xml/www.xml

# Dữ liệu checklist theo tuần (đổi liên tục khi dùng trang setup/), không commit.
xml/day_trade_week.xml

# Báo thức vùng giá trang setup/timer/ (đổi khi đặt/poll), không commit.
xml/timer.xml

# Bot Telegram (token). Xem xml/telegram.example.xml.
xml/telegram.xml

# Trạng thái watcher lệnh (khử trùng cảnh báo), không commit.
xml/mt5_watch.xml